<idea-plugin url="https://sqldalmaker.sourceforge.net//">
    <id>dal-mpe</id>
    <name>SQL DAL Maker</name>
    <category>Database</category>
    <version>1.331.2608</version>
    <vendor email="sqldalmaker@gmail.com" url="https://sqldalmaker.sourceforge.net">
        SQL DAL Maker
    </vendor>

    <!-- Target IntelliJ Platform (2022.2) -->
    <idea-version since-build="222.3345.118"/>

    <depends>com.intellij.modules.lang</depends>

    <description><![CDATA[
<p>
SQL DAL Maker generates the data access layer: DTO, Model, and DAO classes
for PHP, Java, C++, Python, and Go.
</p>

<p>
It works like this:
</p>

<ol>
<li>
<b>XML manifest.</b> The manifest describes what to generate: the DTO and model classes, the DAO
methods, and the SQL behind them.
</li>
<li>
<b>Translate.</b> The manifest is translated into complete source code &mdash; free of any
direct call to PDO, JDBC, or QtSql. Those stay behind a <code>DataStore</code> interface that
you own.
</li>
</ol>

<p>
Reverse engineering serves both. It can write a first draft of the manifest, so you rarely start
from an empty file &mdash; but the manifest is an ordinary text file and can just as well be
written by hand. And it runs again during translation: the generator reads live metadata over JDBC
&mdash; tables, views, and the result sets of your own SQL &mdash; to resolve everything the
manifest leaves open. That is why the database must be up and reachable whenever you generate.
</p>

<p>
The manifest persists, so regeneration is repeatable: when the schema changes, you re-run the
generator and your corrections are still there.
</p>

<p>
Documentation and demo projects:
<a href="https://sqldalmaker.sourceforge.net/">sqldalmaker.sourceforge.net</a>
</p>
    ]]></description>

    <change-notes><![CDATA[
<ul>
<li>[+] toolWindow</li>
<li>[m] since IntelliJ Platform 2022.2</li>
</ul>
    ]]></change-notes>

    <!-- Legacy component tags must remain empty but kept for compatibility -->
<!--    <application-components></application-components>-->
<!--    <project-components></project-components>-->
<!--    <module-components></module-components>-->

    <extensions defaultExtensionNs="com.intellij">

        <!-- Modern Notification Group for NotificationGroupManager API -->
        <notificationGroup id="SDM"
                           displayType="BALLOON"
                           isLogByDefault="true"/>

        <!-- Register DAL file type -->
        <fileType name="com.sqldalmaker.intellij.ui.RootFileType"
                  implementationClass="com.sqldalmaker.intellij.ui.RootFileType"
                  fieldName="INSTANCE"
                  extensions="dal"/>

        <fileEditorProvider implementation="com.sqldalmaker.intellij.ui.RootFileEditorProvider"/>

        <editorTabTitleProvider
                implementation="com.sqldalmaker.intellij.ui.RootFileEditorTabTitleProvider"/>

        <psi.referenceContributor
                implementation="com.sqldalmaker.intellij.references.PsiReferenceContributorCommon"
                language="XML"/>

        <completion.contributor
                language="XML"
                implementationClass="com.sqldalmaker.intellij.references.CompletionContributorCommon"/>

        <toolWindow
                id="SDM"
                anchor="right"
                factoryClass="com.sqldalmaker.intellij.ui.SdmJbToolWindowFactory"
                secondary="false"
        />

    </extensions>

</idea-plugin>
